import { BrowserRouter } from 'react-router-dom';
import { RoutesApp } from './RoutesApp';

const App = () => {
	return (
		<BrowserRouter>
			<RoutesApp />
		</BrowserRouter>
	);
};

export { App };
